#include <stdio.h>
int main()
{
    char st1[20], st2[20];
    int i;
    printf("Enter a string:\n");
    gets(st1);
    for(i = 0; st1[i] != '\0'; i++)
    {
        st2[i] = st1[i];
    }
    st2[i] = '\0';
    printf("Copied string = %s", st2);
    return 0;
}
